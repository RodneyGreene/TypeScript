//JavaScript
let log = function(message) {
    console.log(message);
}

//TypeScript (ArrowFunction)
let doLog = (message) => {
    console.log(message);
}

//Similar to a C# lambda expression
let doLogIt = (message) => console.log(message);

//No parameters?
let doLogNone = () => console.log();

